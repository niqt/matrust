
import { Users } from "users.slint";
import { Chat } from "chat.slint";

export component ChatWindow {
        HorizontalLayout {
            Users {

            }

            Chat { }
        }
}
